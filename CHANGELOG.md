# 变更记录

## 0.9.2 - 2026-09-20

- 员工 Key 明文查看覆盖个人中心和管理端；个人查看验证当前登录密码，管理端查看验证管理员密码。
- 员工 Key 列表新增累计请求数、Token、成本、项目预算占比和项目用量占比。
- 组织详情成员列表新增关联项目数、有效 Key 数、近 30 天 Token / 成本 / 成本占比和预算使用情况。
- 项目 Key 列表新增累计用量、成本、项目预算占比和项目用量占比。
## 0.9.1 - 2026-09-20

- 组织管理列表和概述新增预算进度，可查看组织下项目的总额、已用、预占、可用、使用率和预警项目数。
- 组织概述新增近 30 天请求、Token、采购成本、活跃成员和最后使用时间汇总。
- 项目概述新增预算进度、人员情况、负责人和有效项目 Key 汇总。
- 个人中心可查看本人全部项目 Key，并支持输入当前登录密码后安全查看和复制明文；响应不落缓存，查看动作写入审计。
- 新增组织项目成员与 Key 覆盖补齐脚本：为每个有效组织下的每个 active 项目成员关系补齐项目成员和可恢复项目 Key，支持 dry-run / apply / 幂等复查。
- 新增员工网页初始密码引导脚本：可为无密码员工生成随机初始密码，强制首次登录改密，并生成一次性分发 CSV。

## 0.9.0 - 2026-09-20
## 0.9.0 - 2026-09-20

- 组织与用量组收敛：新增组织类型（经营层、职能部门、区域部门、历史项目组），研发部修正为职能部门，新建工程部和公司经营层，导航从“用量组”切换为“组织管理”，旧用量组地址重定向到组织页面。
- 员工主部门唯一化：一个账号只保留一个 active primary 组织归属；李健、罗晰伊、王宇归入公司经营层，陈旭均、姚依林、何雪岚、石教帅归入研发部，曹庆杰、陈程浩、汪立波、苗美静归入工程部，其余业务员工按区域部门收敛。
- 新增部门预算项目：公司经营层、研发部、工程部各自动创建一个 `DEPARTMENT` 项目；职能部门成员自动同步为部门项目成员，部门预算仍复用项目预算、流水和超限控制。
- 项目模型补齐：项目暴露所属组织与业务/部门分类，支持按组织筛选、显式添加跨部门成员、设置负责人；业务项目创建时必须选择区域部门。
- 员工 Key 收敛为项目 Key：签发只接受 `projectId`，服务端从项目反推组织、校验 ACTIVE 项目成员并拒绝观察者；网关实时校验项目、组织、账号与项目成员，移除项目成员会撤销其项目 Key。
- 项目预算申请补齐：项目负责人和部门负责人可提交申请，平台管理员可“提交并批复”；申请记录标记 `selfApproved`，批复金额为项目总额度而非追加额度。
- 模型权限兼容项目成员：部门成员和跨部门项目成员可访问项目所属组织允许的模型；设备授权也保留项目预算归属。
- 个人中心与 Key 面板文案改为组织/项目视角，个人项目列表同时返回部门预算项目、所属区域项目和显式跨部门项目。

## 0.8.2 - 2026-09-19
## 0.8.2 - 2026-09-19

- 员工 Key 支持安全明文查看：数据库新增 AES-256-GCM 可恢复密文列；管理端默认仍只显示尾号，输入当前管理员密码并验证通过后可查看、复制完整 Key，响应不落缓存并写入审计。新建 Key 自动保存可恢复密文；存量有效项目 Key 已用一次性发放清单完成哈希校验后加密回填。
- 用量组详情新增「关联项目」页签，集中展示区域下项目额度、已用/预占/可用、负责人、职责成员和有效 Key，并可直达项目 Key 列表。
- 用量组成员列表新增「所属项目」与近 30 天组内用量：项目来源区分职责成员和有效 Key，用量包含请求数、Token、采购成本和最后使用时间。

## 0.7.1 - 2026-09-18

- 管理员可修改员工登录邮箱（`PATCH /api/v1/admin/users/:id/email`）：仅管理员、限本组织、禁止修改自己的邮箱，全局唯一冲突返回 409；仅修正登录标识，不使现有会话失效，审计记录前后值。用户详情新增「修改邮箱」入口。
- 员工首次登录引导：管理员设置初始密码的账号，登录后平台强制其设置自有新密码，并可一并把占位邮箱换为真实邮箱（`POST /api/v1/auth/initial-credentials`，需验证当前密码；完成后旧会话失效须重新登录）。数据库新增 `accounts.pending_credential_change` 列（存量默认 false，1 条迁移）。

## 0.7.0 - 2026-09-18

- 管理员可为新建用户设置初始密码，并可对已有账号设置/重置密码（重置会使旧网页会话立即失效）；无密码账号仍可仅凭员工 Key 使用平台模型。
- 用量组成员支持负责人角色：管理员在组详情设置/取消负责人（一人可负责多组，移出组即失权）；负责人在个人中心「我负责的组」只读查看组预算执行、申请记录与组内成员用量。
- 新增预算申请登记：负责人可在线提交申请，管理员可代登记、驳回，或在执行额度调整时关联待批复申请，形成"申请→批复→调整"完整台账；批复金额为批复后总额度。
- 数据库迁移：`group_members.role`（存量默认 MEMBER）与 `group_budget_applications` 新表；升级自动前滚，无需手工数据处理。

## 0.6.0 - 2026-09-17

- 新增个人中心，集中管理基本资料、账号安全、API Keys、我的用量组和我的用量；支持修改本人显示名称并记录审计。
- 移除“我的接入”菜单，旧地址保留直接访问且不自动跳转；API Keys 不新增独立菜单入口。
- API Keys 默认展示本人凭据，管理员可显式切换当前组织管理；增加搜索筛选、分页、详情、历史组筛选和日志入口，复用既有生命周期与单次明文展示规则。
- 本人组展示停用/归档状态及可用模型预览；个人用量按本人和当前组织统计，并保留成本与日志明细跳转。
- 无新增依赖、数据库迁移或网关协议变更；发布范围与验收状态见 [0.6.0 发布说明](docs/release-0.6.0.md)。

## 0.5.1 - 2026-09-16

- 统一管理端原生按钮、链接和文本域的深色主题与键盘焦点样式。
- 修复窄屏侧栏布局及用量组、统计明细宽表格的列挤压问题。
- 修复趋势图图例与横轴标签重叠，并完成 1440、1024、837、390 像素浏览器验收。

## 0.4.0 - 2026-09-15

- 增加项目/部门组、组成员、模型权限、员工 API Key 和人民币成本预算；员工 Key 入口默认关闭，升级不自动归组或变更业务权限。
- 支持组预算并发预占/结算、未知费用核对及按组和凭据分析用量。
- 对象存储客户端替换为 AWS S3 SDK，兼容现有 MinIO 配置与技能包数据，保留上传完整性校验。
- 加固技能 ZIP 扫描和 multipart 输入校验，生产 npm 依赖审计降至 0 漏洞。
- 完成公司 0.3.2 → 0.4.0 升级、隔离迁移及配对数据库恢复启动演练；验证结果及未覆盖事项见 [发布记录](docs/release-0.4.0.md)。

## 0.3.1 - 2026-08-30

- 修复 Skills catalog 将 Prisma `BigInt` 序列化为字符串的问题，`sizeBytes` 现在稳定返回 JSON number，满足 UCLI 客户端目录合同。
- 模型目录新增稳定的 Gateway 协议能力列表；Bootstrap 与 Gateway 模型列表保持一致，并用 `model_protocol_unavailable`、`model_channel_unavailable`、`upstream_unavailable` 区分路由失败。
- 新增独立 UCLI 模型协议能力升级交接文档；客户端不再假设模型列表首项支持 Responses。
- 修复 Bootstrap 下发 `contextSize: null` 导致 UCLI 注册后无法加载服务端能力：公共模型发布时要求正整数上下文长度，并隔离历史无效目录项。
- 修复设备 Refresh 成功与错误响应缺少 `Cache-Control: no-store` 的协议违约，保持 UCLI 客户端敏感响应校验 fail closed。
- 设备授权改为独立的一次性连接 URL：URL 默认有效 7 天，授权默认永久有效；两者有效期独立管理，重新生成 URL 会立即撤销旧 URL。
- 管理端支持查看、重新生成和追踪 URL 历史；授权绑定设备后不再生成或兑换 URL。
- 升级新增设备授权链接历史与签发顺序迁移（`202608270001`、`202608270002`、`202608270003`）；升级及恢复必须保持 `MASTER_KEY` 连续，并按数据库备份与上一版镜像成对回滚。

## 0.3.0 - 2026-08-27

- 支持平台预创建无密码普通成员，并按成员创建一设备一授权令牌；同一成员可获得多个授权以注册多个设备。
- 增加授权有效期、禁用、启用、延期、软删除、浏览器预览与 `ucli://` 唤起，以及 refresh token 轮换和授权有效期同步。
- UCLI 未连接、服务端不可达或授权失效时，本地能力、本地数据和本地会话保持可用；服务端模型、技能及后续能力独立降级。
- **破坏性变更：旧邀请、设备码和旧设备 refresh token 全部失效**；升级前必须备份数据库，旧版应用二进制不能单独回滚。

## 0.2.0 - 2026-08-24

- 完善渠道、渠道密钥、渠道模型、公共模型与成本规则的完整管理能力。
- 支持渠道发现地址配置、模型绑定、按模型厂商聚合展示与模型对话测试。
- 采购成本统一使用人民币，支持基础价格、分时价格及公共目录成本对齐。
- 增加渠道、模型、成本与内部用户使用情况统计分析。
- 增强上游 URL 校验、运行时归档隔离、渠道健康检测与异常密钥隔离。
- 增加目录生命周期、人民币成本和价格启停相关数据库迁移。
- 增加独立采购成本工作台，支持厂家/模型/渠道聚合、周时间轴、规则模板、渠道时区日期换算、成本试算及归档恢复。
