# 0.5.1 — 管理端视觉与响应式修复

## 变更范围

- 统一原生按钮、链接和文本域的深色主题及键盘焦点样式。
- 900 像素以下使用顶部多列导航，宽表格在自身容器内横向滚动。
- 调整趋势图图例、绘图区和横轴标签，避免内容重叠。
- 相比已部署 0.5.0（`60e0222`），无服务端接口、数据库 schema、迁移或依赖变化。

## 发布约束

生产 `conf/.env` 仅允许将 `VERSION=0.5.0` 更新为 `VERSION=0.5.1`，其他配置及密钥保持不变并校验。交付包不包含 `.env`、凭据或数据库备份。

升级前保留 0.5.0 镜像及文件并创建新鲜数据库备份。使用 `./install.sh update`，部署后核对镜像 ID、迁移状态、四个容器、两个健康端点和管理端页面。

实现与本地浏览器验收见 [管理端视觉修复验收记录](verification/2026-09-16-admin-visual-fixes.md)。发布源提交、离线包和生产验收结果由本次发布归档记录保存。

## 发布验证（2026-09-16）

发布源提交为 `674845500d9a3bb8f41219e8490128cedf485596`。干净 `npm ci` 与 `npm run db:generate` 后，使用一次性本地 PostgreSQL 16、Redis 7 和 MinIO 执行 `npm run verify`：125 个测试文件、806 项测试全部通过，无跳过；语句/行覆盖率 96.07%、分支 87.72%、函数 97.82%。服务端构建、709 模块管理端构建和 452 项许可证检查通过；保留既有大于 500 kB 的管理端 chunk 提示。

首次未配置本地 PostgreSQL/Redis 的门禁有 61 项集成测试按条件跳过并导致覆盖率失败；未降低门槛。配置专用本地服务、部署全部 16 条迁移后完整重跑通过。

## 公司服务器发布记录（2026-09-16 16:39，北京时间）

- 目标：`root@10.44.100.100:/data/ucli-server`。
- 离线包：`ucli-server_0.5.1_20260916_6748455.tar.gz`，213179377 字节，SHA-256 `172b98d99d91bca1d334f812005eacdd40ba05cc90a9a2da2ee8a9df58496668`。
- Runtime：`sha256:ea312e168a251e06f3fb27bbef18fb8c1c7868465c4b74ebc2c5bf1ed0da3149`。
- Web：`sha256:ae44183daca3784418b86c893a6ab4f203846fb22acc801d8c58068337c09b7d`。

包内严格为 9 个交付文件，内部 SHA-256 全部通过，不含生产 `.env`、凭据或数据库备份。镜像均带 `0.5.1` 版本和发布源 revision 标签；Runtime 内版本为 `0.5.1`，Web 镜像包含本次视觉修复 CSS。

升级前备份位于 `/data/ucli-server/backups/production-051-6748455`。数据库备份 `ucli.dump` 为 650555 字节，SHA-256 `1854e53cb99864ce0993e293b3f35e065f6179fa21347085c033c4bfe7b9b944`；已在隔离 PostgreSQL 16 中完整恢复，16 条迁移记录与生产逐项一致。`0.5.0` 发布文件、镜像清单、配置哈希和回退脚本均保留；回退脚本 SHA-256 为 `77834f3a4a63065fd74e852e6d145eee284211de7b5c750415f6b3a196dd2d37`。

生产 `conf/.env` 仅将 `VERSION=0.5.0` 改为 `VERSION=0.5.1`，正向及逆向整文件哈希均通过，其他配置和密钥保持不变。`./install.sh update` 报告 16 条迁移且无待执行项；升级前后迁移名称和校验值完全相同。

四个应用容器均运行 `0.5.1` 清单中的实际镜像 ID，重启次数为 0；API 与 Gateway 为 healthy，PostgreSQL/Redis 依赖均为 ok。工作站访问主页及两个健康端点均为 HTTP 200；部署 CSS 资源包含深色文本域回退规则。服务总览、用量组和统计分析接口在匿名状态均返回预期 HTTP 401。

浏览器已确认生产登录页正常加载且深色样式完整。浏览器会话已退出登录，因此未输入或传输管理员凭据，登录后的三页交互验收保留为人工确认项；未创建生产数据、Key，也未发起付费模型调用。
