# 0.4.1 — DeepSeek 缓存兼容与成本核算

## 变更范围

- 组预算允许官方 DeepSeek Anthropic 端点的合法文本/工具缓存标记原样透传，要求全部候选渠道满足条件；不按渠道名称判断，不开放其他供应商的缓存写入计费。
- 共享用量解析支持 DeepSeek `prompt_cache_hit_tokens`，标准字段（含零值）优先；总输入不重复累加缓存命中。
- 预占仍保守估计，结算按上游用量和请求时人民币价格快照；未知缓存写入成本保留待核对。
- 相对生产 0.4.0，无数据库 schema、迁移或依赖版本变更。公司价格配置和历史费用不随发布改写。

## 发布要求

本地完整 `npm run verify` 与 HTTP 集成检查通过后，才从已提交的干净 main 打包。发布包记录精确提交和镜像 ID，不含 `.env`、密钥或数据库备份。

生产仅允许 `conf/.env` 的 `VERSION=0.4.0` 改为 `VERSION=0.4.1`；通过逆变换哈希证明其他字节不变。升级前保留旧镜像、发布文件和新鲜数据库备份，并在隔离 PostgreSQL 验证恢复；使用 `./install.sh update`，核对 16 条既有迁移校验值未变。

真实验收使用独立授权的短期 Key，不复用或导出现有 Key，不调整组预算。对比上游响应 usage、平台使用日志和请求预算账目；这些证据不等于厂商最终账单。

## 执行状态

2026-09-15 本地验证：111 个测试文件、696 项全部通过，无跳过，行覆盖率 96.01%、分支覆盖率 85.78%。`group-http.mjs`、`employee-key-http.mjs`、`skills-upload-http.mjs` 三组 HTTP 集成检查通过，使用专用本地 PostgreSQL、Redis 和临时 MinIO，不访问付费上游。只读代码复核未发现严重或重要问题。

之前的 Prisma ESM 加载超时在干净安装后未复现，未放宽测试限时。Docker 内官方 Prisma 二进制下载曾两次连接重置；确认主机与容器均可完整下载后，原始全量测试通过，未改依赖源、Dockerfile 或关闭校验。

完整 `npm run verify` 退出码 0：类型检查、覆盖率测试、服务端/管理端构建及 452 项许可证检查全部通过。管理端仍有既有的大分包提示，不阻断构建。

## 公司服务器发布记录（2026-09-15 17:58，北京时间）

- 目标：`root@10.44.100.100:/data/ucli-server`。
- 已部署源提交：`2eb0b4ad38a1bedbb2cd23ef7821b1b20118060f`，main 已推送。
- 离线包：`releases/ucli-server_0.4.1_20260915_2eb0b4a.tar.gz`，208663955 字节。
- 包 SHA-256：`bf45faae67d080ffbdbd8b477db5b271956874237899ef9495141f3f934426c7`。
- Runtime：`sha256:c9636a556636d0b845c895bfe3564a8cbbe5c3030abfe5c2381c73205c6631f9`。
- Web：`sha256:1ec93aabcb562f50dbc8badf61f32f7cf66aab1ec8ed6cc6ed27b8242927dcb6`。

运行镜像首次构建也遇到 Prisma 下载重置；仅对重试命令清空代理 build args 后成功，未改 Dockerfile、依赖、镜像标签或系统代理配置。通过精确提交的 `git archive` 构建，恢复打包前逐文件比对归档内容，两个镜像的 revision/version 标签均验证通过。包仅含预定 9 个交付文件，本地/远端 SHA256SUMS 全部通过。

生产依赖审计（官方 npm registry、`--omit=dev`）0 项漏洞。开发测试依赖有 4 项告警：glob 1 项 high，Vitest 链路 3 项 moderate；不包含于运行镜像的生产依赖，本次未混入跨大版本升级。管理端仍有既有分包大小提示。

发布前备份：`/data/ucli-server/backups/production-041-2eb0b4a/ucli.dump`，SHA-256 `9d02345a6195a4f025c54a7576e860bec693afbff1b8d8c2678cdb65a85b69f3`。新鲜备份已恢复到专用内部网络中的既有隔离 PostgreSQL，验证后停止该容器。旧 0.4.0 镜像、原交付文件、原配置哈希均保留；不复制或输出生产密钥。

预检发现数据库部分历史迁移校验值使用 CRLF，Git blob 使用 LF；逐项验证 16 条迁移与部署的 b1f5312 源内容完全一致，数据库哈希均对应同一内容的 LF/CRLF 版本，没有修改历史迁移或数据库校验值。`install.sh update` 报告 `16 migrations found`、`No pending migrations to apply`；备份、隔离恢复、升级后的迁移记录逐字一致。

部署/回退使用同一排他锁。人工回退脚本保存在上述备份目录的 `rollback.sh`，只适用于本次无 schema 变化的 0.4.1 → 0.4.0；执行前拒绝迁移记录或其他配置漂移，不自动回退未知失败。部署脚本 SHA-256 `f19b0480789c1c66e1dd35960f6a4c562a433f1b7fffbbb507f35c4a83e7c825`，回退脚本 SHA-256 `7921ba394b6d77f286b668c57cecd83fc5caeb6cea0af7c1342402f8573b4d7d`，执行前与本地已审查文件逐项校验；未实际执行生产回退。

配置只变更 `VERSION=0.4.0` → `VERSION=0.4.1`，逆变换整文件哈希通过，其他配置与密钥不变。API/Gateway/Worker/Web 实际镜像 ID 与清单一致、运行正常，API/Gateway 健康检查的 PostgreSQL/Redis 均为 ok；外部页面 200 且包含 SPA 根节点。无凭据及无效员工 Key 的模型目录访问均返回 401。

17:59:44 再次回读价格审计：4 个 Flash 映射均为新高峰价，两个公共底价版本正确，旧规则/旧价格记录保持不变；样例采购费用 ¥0.001232，下一切换点 18:00。

18:01:22 只读复核：4 个映射已自动选中空闲价（输入 1、缓存 0.02、输出 4 元/百万 Token），相同样例费用变为 ¥0.000616，下一切换点为次日 09:00。没有手动调整服务器时间或价格。运行中的 Gateway 编译代码也验证了官方缓存标记可接受、代理端点被拒绝，以及总输入 1000/缓存命中 800 的解析；该检查没有出站请求。

### 仍待验收

生产基础设施发布完成。用户仅确认生产 VERSION 变更，尚未确认小额临时 Key 验收；因此未创建 Key、未调整组预算、未发起付费请求。真实 Claude Code 默认缓存对话/流式/只读工具调用，以及同一请求的上游 usage、使用日志和组预算对账，仍待授权后执行，不能以本地或容器内函数验证替代。
