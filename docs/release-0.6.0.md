# 0.6.0 — 个人中心与平台 API Keys 管理

状态：2026-09-17 已部署公司服务器，基础设施与无凭据 HTTP 验收通过；登录后的页面交互验收待确认。2026-09-18 版本归档使用 `main` 保存发布记录，`v0.6.0` 标签固定到已部署的发布源提交。功能源提交为 `37126c6`；本次发布源提交为 `016679a2576fa99e22695b585bfba35e536f6970`。

## 变更范围

- 新增 `/profile`，包含基本资料、账号安全、API Keys、我的用量组、我的用量。允许修改本人显示名称，组织、邮箱和角色由管理员维护。
- 移除“我的接入”菜单；旧 `/my-access` 仍可直接访问，不自动跳转。API Keys 位于个人中心，不新增独立侧栏菜单。
- API Keys 默认本人视图，管理员显式切换到当前组织管理；普通成员仅查看、撤销本人 Key。搜索、状态/员工/组筛选、分页、详情与日志跳转复用既有授权范围。
- 历史组筛选独立于当前列表页；创建 Key 仍只选择员工有效成员组。完整 Key 仅创建时显示一次，关闭或离开页面后清空。
- 个人组保留停用、归档状态，模型预览使用现有组权限；个人用量对管理员同样强制限定本人和当前组织，并保留人民币成本、估算成本与待核对提示。
- 密码修改复用既有接口与会话失效规则；不新增账号激活、首次设密或密码找回流程。未设置密码的员工仍由管理员管理 Key。

## 升级兼容性

相对 0.5.1，无数据库 schema、迁移、依赖、预算结算或网关协议变更；不重新签发现有 Key，不自动变更模型权限或员工 Key 功能开关。上游渠道密钥仍在渠道管理。

身份读取接口增加安全展示字段；新增本人资料更新、本人组/模型/用量读取、当前组织 Key 集中查询。既有 Key 列表保持原分页字段并追加关系摘要；本人/指定员工列表追加仅含组 ID 与名称的历史筛选选项。

版本号已同步到 `package.json`、`package-lock.json`、`conf/.env.example` 和部署镜像加载示例。未修改实际 `.env` 或生产配置。

## 已完成验证

功能源提交 `37126c6` 的本地完整验证：129 个测试文件、836 项测试全部通过，无跳过；类型检查、服务端与管理端构建、452 条许可证检查通过。行/语句覆盖率 96.07%、分支 87.73%、函数 97.82%。完整测试使用两个 worker，原因及浏览器、真实 HTTP 验收边界见 [个人中心验收记录](verification/2026-09-17-personal-center.md)。

本次发布准备仅修改版本元数据和文档；版本一致性、lockfile 依赖不变、相对 0.5.1 的迁移及 schema 不变检查通过。重新执行类型检查、两端构建和许可证检查均通过，管理端构建 714 个模块，许可证检查 452 条记录，`git diff --check` 通过。没有将上一轮功能测试记作本轮重新执行。

保留既有管理端大于 500 kB 的 chunk 提示。生产登录后的页面验收尚未执行。

## 发布操作流程

1. 使用下方已验证离线包，先重新核对包哈希及目标实际版本；如源代码或配置范围变化，重新明确发布来源与验证范围。
2. 依照 [部署说明](../DEPLOY.md) 核对目标当前版本、健康、空间、外部网络和 Compose，创建新鲜数据库备份并验证隔离恢复，保留上一版镜像与发布文件。
3. 如目标仍为 0.5.1，升级配置仅调整 `VERSION=0.5.1` 至 `VERSION=0.6.0`；保留其他配置字节及 `MASTER_KEY`，验证配置连续性。通过 `./install.sh update` 升级，核对 16 条既有迁移无新增待执行项。
4. 验证四个应用容器实际镜像、API/Gateway 健康及外部页面；登录后验收个人中心、角色范围、个人用量和旧地址不跳转，再补充实际发布日期与部署记录。

升级或回退均遵循部署说明。无 schema 变化不代表可以忽略备份和配置连续性检查；发布准备阶段未执行生产操作，本次实际升级见下方记录。

## 本地合并与离线包验证（2026-09-17）

同步远端后确认本地与远端 `main` 均为 `13048b0`，在独立发布工作区将本地 `main` 快进到 `016679a`，保留原工作目录和未提交内容。没有推送 Git 远端或创建发布标签。

在新工作区执行干净 `npm ci` 和 Prisma Client 生成，使用本地 PostgreSQL 16、Redis 7、MinIO 重新运行等价完整门禁：类型检查、`npm run test:coverage -- --maxWorkers=2`、服务端构建、管理端构建及许可证检查。129 个测试文件、836 项测试全部通过，无跳过；行/语句覆盖率 96.07%、分支 87.71%、函数 97.82%；452 条许可证记录检查通过。

首次门禁因测试 PostgreSQL 容器重启重复执行 `initdb` 而无法连接，产生 59 项失败。保留已有测试数据，以直接启动 PostgreSQL 的新本地容器恢复服务，确认 16 条迁移完整后从头重跑门禁通过。未修改应用、测试断言、超时或覆盖率门槛；本地测试服务在验收后停止。

- 发布源：`016679a2576fa99e22695b585bfba35e536f6970`，从该提交直接 `git archive` 到全新构建目录。
- 离线包：`releases/ucli-server_0.6.0_20260917_016679a.tar.gz`，208731628 字节。
- 包 SHA-256：`e8a495c6bd783445e26a95ac2f7067e601d1d3ffe17b450072ceb97021b6c836`。
- Runtime：`ucli-server-runtime:0.6.0`，ID `sha256:abc67c700b1c2ac636f6f82c95821a7000a045d6e5a06574002c299a00da39d1`。
- Web：`ucli-server-web:0.6.0`，ID `sha256:084e1eaab07eb919be9b74ed577ca4440afe51b5b34c8f4b4ce9fc4cecec4b96`。

镜像均为 Linux amd64，并带精确 version/revision 标签。Runtime 离线冒烟确认包版本、个人中心控制器及无 `.env`；Web 确认入口文件和个人中心资源存在。镜像归档保留安装所需版本标签，导出配置的 SHA-256 与已核对镜像 ID 一致。

离线包严格包含规范的 9 个交付文件；重新解压后逐项核对内部 SHA256SUMS，归档路径及文件白名单检查通过，无实际 `.env`、凭据或数据库备份。打包脚本经独立审查，来源绑定和镜像归档身份验证补齐后无待处理发现。该阶段尚未上传、执行生产升级或进行生产登录验收。

## 公司服务器发布记录（2026-09-17 11:14，北京时间）

目标：`root@10.44.100.100:/data/ucli-server`。升级前新鲜检查确认运行 0.5.1，四个应用容器正常、API/Gateway 健康；上一版镜像及全部发布文件哈希有效，Compose 2.29.7、外部网络和磁盘空间满足要求。

使用上方已验证离线包，未重新构建。上传目录为 `/data/ucli-server/incoming/production-060-016679a`，远端包 SHA-256 与本地完全一致；提取前验证归档成员严格为 9 个交付文件，提取后验证内部校验和。新镜像的 ID、version/revision 标签在切换前核对通过。

新鲜备份目录：`/data/ucli-server/backups/production-060-016679a`。其中 `ucli.dump` 为 673157 字节，SHA-256 为 `b73790299a9583d0febc8b5cc63e0f88845d043ac827ff6ab192fbb59fd266bc`。备份在与生产相同 PostgreSQL 镜像的隔离容器中成功恢复；数据库版本、扩展及 16 条迁移名称/校验值与生产完全一致。恢复容器仅接入既有内部网络，无宿主机端口，验收后停止并保留恢复数据供核查。0.5.1 发布文件、镜像和配置哈希均保留。

生产 `conf/.env` 仅将 `VERSION=0.5.1` 调整为 `VERSION=0.6.0`；修改前哈希、反向替换整文件一致性、升级后预期哈希均通过，其他配置字节和密钥保持不变。升级脚本持有排他部署锁，调用 `./install.sh update`；日志报告 `16 migrations found`、`No pending migrations to apply`，升级前后迁移记录逐字一致。

四个应用容器均运行本记录所列 0.6.0 Runtime/Web 实际镜像 ID，重启次数为 0；API/Gateway 为 healthy，两个健康接口的 PostgreSQL/Redis 依赖均为 ok。工作站访问两个健康端点成功，主页及全部 9 个 JS/CSS 资源（共 10 个文件）与已验证 Web 镜像逐字节一致。身份、个人组、个人用量、组织 Key、本人 Key 五个接口在匿名状态均返回 HTTP 401；`/profile` 与 `/my-access` 均返回预期 SPA，未发生 HTTP 重定向。

升级与回退脚本经独立审查；执行前验证上传脚本的固定 SHA-256，回退脚本也在复制前验证其固定摘要。部署脚本 SHA-256 为 `c6f9acd4f69f96e7a93fd5efbd93ea07007e7b7d46634cb3d73f0e5b9a2a4e3f`；回退脚本保存在备份目录的 `rollback.sh`，SHA-256 为 `5f07932926ffae95623c1a382747163c55d47135839b6b46dba7b77a61ff07a7`。回退脚本仅适用于本次无 schema 变化升级，要求迁移记录与非 VERSION 配置无漂移；未执行生产回退。

### 验收边界

浏览器创建/读取与刷新多次超时；备用 DOM 接口取得生产登录页，但未能稳定刷新，未输入管理员凭据。登录后的个人中心五页签、组织/本人 Key 切换及旧地址的客户端行为仍需管理员登录确认。HTTP 静态资源一致性与本地浏览器测试不能代替这项生产交互验收。未创建生产账号或 Key，未修改业务权限、预算或价格，未发起付费模型调用。

## 版本归档（2026-09-18）

- 版本标签：[`v0.6.0`](https://github.com/AllernChen/ucli-server/tree/v0.6.0)，固定到发布源 `016679a2576fa99e22695b585bfba35e536f6970`，与离线包及生产镜像的源码一致。
- 发布记录：`main` 保留离线包验证、公司服务器部署和本次归档记录；发布源之后仅补充 `CHANGELOG.md` 与本文件，不改变已部署代码。
- 离线包、镜像摘要、备份及回退依据见上文；本次归档不重新打包或部署。
- 生产登录后的页面交互验收仍为待确认，版本归档不代表该项验收通过。
