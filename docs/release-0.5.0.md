# 0.5.0 — 内部采购成本与运营管理

## 变更范围

- 使用日志：员工、用量组、渠道、模型等组合筛选，安全的历史价格与费用明细，筛选结果 CSV 导出。
- 统计分析：按请求与渠道分摊分别汇总，缓存用量、人民币采购成本、员工和用量组分析，支持明细下钻。
- 用量组：预算风险筛选、成员与 Key 管理信息、预占和结算明细，以及完整分析入口。
- 服务总览：真实服务健康、成本和用量摘要、异常渠道及预算风险提示；遵循现有角色权限。
- 相比已部署 0.4.1（`2eb0b4a`），无数据库 schema、迁移或依赖变化，不调整采购价格、历史费用、预算或模型权限。

## 发布约束

用户已授权发布 0.5.0，并仅将生产 `conf/.env` 的 `VERSION=0.4.1` 改为 `VERSION=0.5.0`。其他字节及全部密钥保持不变，通过逆变换整文件哈希校验。

从干净、已提交的源码构建版本化镜像，交付包不包含生产配置、密钥或数据库备份。升级前保留 0.4.1 镜像及文件，创建新鲜数据库备份并验证隔离恢复，核对 16 条既有迁移及其内容未变。使用 `./install.sh update`，部署后核对镜像 ID、迁移状态、健康和页面。

已合并功能的本地验证见 [验证记录](verification/2026-09-15-admin-cost-operations.md)。发布验证与生产状态见下文，区分基础设施检查与登录后的页面交互验收。

## 发布验证（2026-09-16）

干净 `npm ci` 后依照仓库 CI 执行 `npm run db:generate`，再依次运行类型检查、单进程完整覆盖率测试、服务端与管理端构建、许可证检查，整组退出码 0。124 个测试文件、803 项测试全部通过，无跳过；行与语句覆盖率 96.07%、分支 87.73%、函数 97.82%。管理端保留已有的 889.31 kB 分包提示，452 项许可证检查通过。

首次干净安装后的类型检查因未生成 Prisma Client 失败；按现有生成步骤恢复，无源码、依赖或测试限时修改。生产只读检查确认 16 条既有迁移的数据库校验值与相同源码的 LF/CRLF 版本逐项一致，不改写迁移历史。

## 公司服务器发布记录（2026-09-16 10:49，北京时间）

- 目标：`root@10.44.100.100:/data/ucli-server`。
- 运行版本：`0.5.0`；源提交：`60e02221cee990c63ff04c43746e59dde8d7bb18`。
- 离线包：`releases/ucli-server_0.5.0_20260916_60e0222.tar.gz`，208717961 字节。
- 包 SHA-256：`14add2f3efd9da04891061b272cf6af9bea05362a6e497850ff407c07ecc3687`。
- Runtime 镜像：`sha256:4382d713d58f8ae4b8d083d3abdd283a4c76152e4efb83d2c2c299ded4095548`。
- Web 镜像：`sha256:1c8ee36cfbb9cb68bdf837cf913a469dd91007dcdc23922169ad016fffb3caa3`。

镜像从干净 main 的精确 `git archive` 构建，包含 revision/version 标签。包内严格为 9 个交付文件，不含 `.env`、密钥或备份。本地与远端包哈希、归档文件清单、各文件 SHA256SUMS 均通过；部署及回退辅助脚本经独立只读审查，无严重或重要问题。

生产依赖构建审计为 0 项漏洞；开发测试依赖仍有 4 项告警（1 high、3 moderate），未包含于运行镜像的生产依赖，本次未调整依赖。管理端仍有既有分包体积警告。

新鲜数据库备份：`/data/ucli-server/backups/production-050-60e0222/ucli.dump`，SHA-256 `6e7a33f5c1077e06ace1d03b10f382a741b270ceda7f7dda39da6bde2b6858b1`。已恢复到既有专用内部网络的隔离 PostgreSQL，数据库版本、扩展、16 条迁移记录与生产匹配；验证后隔离容器已停止，数据保留。

原 0.4.1 发布文件、镜像和配置哈希保留。生产仅 `VERSION=0.4.1` → `VERSION=0.5.0`，逆变换整文件哈希及升级后哈希均通过，其他配置与密钥不变。未调整采购价格、组预算、模型授权、用户、现有 Key 或历史费用。

升级通过持有排他锁的脚本调用 `./install.sh update`，报告 `16 migrations found`、`No pending migrations to apply`；升级前后迁移记录逐字相同。四个容器实际镜像 ID 与清单一致，均在运行且复核重启次数为 0；API/Gateway 为 healthy，PostgreSQL/Redis 健康均为 ok。

工作站外部 HTTP 验证：主页及两个健康接口 200；全部 6 个管理端 JS/CSS 文件与已验证构建逐字节一致；服务总览、用量组、统计概览、用量分页与模型目录在未认证状态均返回 401，无效员工 Key 访问模型目录亦为 401。首轮探测误用不存在的 `/analytics/summary` 得到 404；读取路由定义后改为 `/analytics/overview`，完整探测重跑退出码 0，无服务端修改。上传预检最后的展示命令曾因 PowerShell stdin 尾部 CR 报错，已用直接 SSH 命令重新校验清单及 RELEASE 后才执行升级。

回退辅助脚本保存在上述备份目录的 `rollback.sh`，仅适用于此次无 schema 变化的 0.5.0 → 0.4.1；要求迁移记录和除 VERSION 外的配置无漂移。部署脚本 SHA-256 `ed40b59b9e45e7f4b07da164362225b136b155ed60dc0d5ce05b24f73fec9cf4`，回退脚本 SHA-256 `61eb1abc31a8211981893ca36176371a9c731d02d74943b4ba2b2b9c91febafb`。未执行生产回退。

### 验收边界

基础设施发布与上述无凭据 HTTP 冒烟检查完成。浏览器创建/读取验收页两次超时并重置工具，无法完成生产登录后的统计分析、用量组、服务总览交互验收；不能用静态文件或本地测试替代这一项。需要管理员刷新生产页面并确认三页展示、筛选及明细跳转。未创建临时生产 Key、未发起付费模型调用，未重复测试本次未改动的模型协议。

本地专用测试 PostgreSQL/Redis 在验证后已停止，数据保留；没有关闭其他应用。本次提交与归档保留在本地 main，未推送 Git 远端。
