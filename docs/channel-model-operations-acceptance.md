# 渠道模型运营优化验收记录

验收日期：2026-08-20  
实现分支：`codex/channel-model-operations`

## 交付范围

- 渠道列表、渠道详情、渠道模型、Key、模型健康和分时采购成本集中维护。
- 模型级单项测试、每渠道最多 20 项的并发批量测试、Worker 定时探测和 30 天记录清理。
- 固定渠道模型多轮对话测试台；消息不持久化，不进行故障切换。
- 网关按实际渠道模型解析、预留、结算并固化公司采购成本。
- 组织、渠道、公共模型、渠道模型、员工和成本规则六个维度的实时分析。
- 平台不包含销售价格、收入、毛利或利润计算。

## 数据库兼容性

- 迁移：`202608200001_channel_model_operations`，开发库 `prisma migrate status` 返回 `Database schema is up to date`。
- 迁移前备份：`F:\projects\ucli-server-backups\ucli-20260820-214013.dump`。
- 迁移后保留 `model_price_versions` 和历史 `usage_logs.cost_usd`；旧公共模型成本作为兼容兜底。
- 迁移后共有 3 条渠道模型、10 条历史用量；7 条历史用量可安全关联渠道模型，10 条均完成成本快照回填。其余 3 条因历史协议与映射不一致而保持空关联，避免错误归因。
- 已确认以下统计索引存在：组织+时间+渠道、公共模型、员工、渠道模型。
- 回滚方式：先部署兼容旧结构的上一版本应用，再恢复上述迁移前 PostgreSQL 备份；不建议在运行库直接手写反向 DDL。

## 自动化验证

执行 `npm run verify`：

- 类型检查通过。
- 34 个测试文件、116 项测试全部通过。
- V8 总覆盖率：语句/行 91.06%，分支 82.87%，函数 85.26%。
- 后端 TypeScript 生产构建通过。
- 管理后台生产构建通过；首屏主包约 175.44 KB，ECharts 统计页按路由拆分约 545.79 KB。
- 434 个依赖记录通过许可证门禁。

专项测试覆盖分时成本、跨午夜窗口、健康状态机、三类上游协议、固定渠道测试隐私、最高成本预留/实际成本结算、权限范围和 SQL 参数化。

## 真实环境验收

- 四组统计查询（overview、timeseries、breakdown、filter-options）已在迁移后的本地 PostgreSQL 上执行成功。
- 成本规则维度可返回采购成本、加权输入/输出单价和规则时段；被删除或兜底规则可显示历史来源。
- 应用内浏览器已验证渠道列表、渠道详情、渠道模型与 7×24 成本编辑器、公共模型发布检查、固定渠道模型测试台、统计指标、ECharts 趋势图和成本规则维度表。
- 浏览器控制台未发现错误。验收过程中发现并修复趋势图在异步数据到达前条件挂载导致不初始化的问题。

## 性能说明

当前开发库数据量较小，PostgreSQL 会选择顺序扫描，不能据此声称已达到百万用量日志下的 P95 指标。发布到大数据量环境前，应使用脱敏性能数据执行 `EXPLAIN (ANALYZE, BUFFERS)` 和 P95 压测；所需复合索引已随迁移创建。本次未向共享开发库灌入 100 万条模拟用量，避免污染现有业务数据。
